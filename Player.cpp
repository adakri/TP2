#include <Player.h>

Player::Player(int id, double size, std::vector<double> v0, int nbThrows, int nbStones)
{
};

double& get_id()
{
    return id()
};

double& get_size()
{
    return size()
};

std::vector<double&> get_speed()
{
    return vo()
};



void Play()
{
    vector<double> y;
    vector<double> vx;
    vector<double> vy;
    double temp1;
    double temp2;
    double t;
    double cte;

    double y0 = player.size()s

    double vx0 = 6. + 0.5 * 0.5 * distribution(generator) + 0.1 * 0.1 * 
    double vy0 = 4. + 0.5 * 0.5 * distribution(generator) + 0.1 * 0.1 * distribution(generator);
    // Mass
    double mass = 10 + 3 * 3 * distribution(generator); // g
    //constant
    cte = coeff / mass;
    t = 0.;
    y.push_back(y0);
    x.push_back(x0);
    vy.push_back(vy0);
    vx.push_back(vx0);
}